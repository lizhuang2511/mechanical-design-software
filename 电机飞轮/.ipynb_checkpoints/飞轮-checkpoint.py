import numpy as np
import matplotlib.pyplot as plt
plt.rcParams['font.sans-serif']=['SimHei'] #用来正常显示中文标签
plt.rcParams['axes.unicode_minus']=False #用来正常显示负号
from 电机计算新 import 电机能量
class 飞轮计算(电机能量):
    def __init__(self):
        a=1
    def 飞轮惯量(self):
        self.工件变形功/self.飞轮角速度()**2*self.压力机效率()*self.飞轮不均匀系数()
        return
    def 飞轮角速度(self):
        return
    def 压力机效率(self):
        return
    def 飞轮不均匀系数(self):

        return